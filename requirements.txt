# Runtime — ML & API core
pandas~=2.2
numpy~=1.26
joblib~=1.4
scikit-learn~=1.6.0
xgboost~=2.1
shap~=0.46
fastapi~=0.115
uvicorn~=0.30
pydantic~=2.9

# Rate limiting (in-process, no Redis required for scale-to-zero)
slowapi~=0.1

# Structured logging (stdout JSON → Log Analytics KQL)
python-json-logger~=2.0

# Azure SDK
azure-identity~=1.19
azure-keyvault-secrets~=4.9
azure-storage-blob~=12.23

# Application Insights tracing (Phase 2)
opencensus-ext-azure~=1.1

# Testing & tooling
pytest~=8.3
httpx~=0.27
matplotlib~=3.10
seaborn~=0.13

# Static analysis (used in CI, safe to install locally too)
ruff~=0.6
mypy~=1.11
bandit~=1.8
pip-audit~=2.7
